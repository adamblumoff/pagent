{
  "name": "pagent",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "sideEffects": false,
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "pagent": "./dist/cli.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./connector": {
      "types": "./dist/connector-entry.d.ts",
      "import": "./dist/connector-entry.js"
    }
  },
  "scripts": {
    "build:sdk": "tsc -p tsconfig.build.json",
    "typecheck:cloud": "tsc -p tsconfig.cloud.json",
    "typecheck:consumers": "tsc -p test/consumers/nodenext && tsc -p test/consumers/bundler && tsc -p test/consumers/web-runtime",
    "build": "pnpm build:sdk && pnpm --filter pagent-demo build && npm --prefix relay run build",
    "cli": "pnpm build:sdk && node dist/cli.js",
    "demo": "pnpm build:sdk && pnpm --filter pagent-demo dev",
    "test": "vitest run && npm --prefix relay test",
    "setup:e2e:frameworks": "pnpm --dir test/e2e/frameworks install --frozen-lockfile",
    "typecheck:e2e:frameworks": "pnpm --dir test/e2e/frameworks --recursive typecheck",
    "test:e2e:frameworks": "pnpm build:sdk && pnpm --dir test/e2e/frameworks --recursive build && vitest run --config vitest.e2e.config.ts",
    "smoke:bun": "pnpm build:sdk && bun test/runtime/bun-smoke.mjs",
    "typecheck": "pnpm typecheck:cloud && pnpm build:sdk && pnpm typecheck:consumers && tsc --noEmit && pnpm --filter pagent-demo typecheck && npm --prefix relay run typecheck"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "railway": "3.11.0",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
