{
  "name": "pagent",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "sideEffects": false,
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "pagent": "./dist/cli.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./connector": {
      "types": "./dist/connector.d.ts",
      "import": "./dist/connector.js"
    }
  },
  "scripts": {
    "build:sdk": "tsc -p tsconfig.build.json",
    "typecheck:cloud": "tsc -p tsconfig.cloud.json",
    "typecheck:consumers": "tsc -p test/consumers/nodenext && tsc -p test/consumers/bundler && tsc -p test/consumers/worker && tsc -p test/consumers/deno && tsc -p test/consumers/vercel",
    "build": "pnpm build:sdk && pnpm --filter pagent-demo build && npm --prefix relay run build",
    "cli": "pnpm build:sdk && node dist/cli.js",
    "demo": "pnpm build:sdk && pnpm --filter pagent-demo dev",
    "test": "vitest run && npm --prefix relay test",
    "test:e2e:frameworks": "pnpm build:sdk && pnpm --filter './test/e2e/frameworks/**' build && vitest run --config vitest.e2e.config.ts",
    "smoke:bun": "pnpm build:sdk && bun test/runtime/bun-smoke.mjs",
    "typecheck": "pnpm typecheck:cloud && pnpm build:sdk && pnpm typecheck:consumers && tsc --noEmit && pnpm --filter pagent-demo typecheck && pnpm --filter './test/e2e/frameworks/**' typecheck && npm --prefix relay run typecheck"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  }
}
