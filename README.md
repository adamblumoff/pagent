# Pagent

Pagent turns deterministic software events into coding-agent work in the developer's existing environment.

The initial product thesis is intentionally narrow:

> When a typed event occurs, start a coding-agent thread against the right repository with the relevant context.

This repository is at the concept stage. The first implementation will validate one end-to-end path before committing to an event transport, agent runtime, or hosted service.
