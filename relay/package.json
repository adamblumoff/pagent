{
  "name": "pagent-relay",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "prebuild": "pnpm --dir .. build:sdk",
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/src/index.js",
    "test": "pnpm build && node --test --test-reporter=spec dist/test/*.test.js",
    "test:postgres": "pnpm build && node --test --test-reporter=spec dist/test/postgres-store.test.js",
    "pretypecheck": "pnpm --dir .. build:sdk",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "pagent": "workspace:*",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/pg": "^8.15.5",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
